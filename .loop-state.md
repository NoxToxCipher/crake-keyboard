# Perf-fix loop state

Cron job: a83d9396 (*/8 * * * *, session-only). Hard stop: 12 iterations or all items done.
Branch: claude/app-performance-review-kvikrq. Method per iteration: guards/oracles first
(run against OLD code), then new code, then oracles + cargo test --workspace + perf bench,
then commit + push. Partial work is recorded here, never pushed unverified.

## Iterations used: 10 / 12

## LOOP COMPLETE (2026-09-02) — all 10 items done; all 4 deferred sub-parts now shipped & compiled.

Fully done (verified by oracle and/or cargo, pushed): 1, 2, 3, 4, 7, 9, 10.
Fully done incl. their once-deferred sub-parts (build-verify session, 2026-09-02): 5 (5a+5b),
6 (lock-split), 8 (off-main bitmap decode).

Deferred sub-parts — ALL now IMPLEMENTED & VERIFIED against a real build (the environment network
policy was changed to allow dl.google.com/maven.google.com, so :app:compileDebugKotlin +
:app:testDebugUnitTest now run):
 - 5a: DONE — nativeGetTouchOffsets JNI batch (HitTester::offsets_for), guard batch_offsets_oracle.rs.
 - 5b: DONE — system user-dictionary in-memory cache + ContentObserver on UserDictionary.Words.
 - 6:  DONE — touch model behind its own Arc<RwLock> inside NlpEngine; record path takes engine.read();
       guard touch_model_lock_split.rs (no deadlock + frozen-snapshot determinism).
 - 8:  DONE — AsyncClipThumbnail: off-main produceState(IO) decode + bounds pre-pass + inSampleSize;
       guard ClipInSampleSizeTest.kt.

Standing status for the whole branch: the FULL app now BUILDS AND PACKAGES. :app:assembleDebug
BUILD SUCCESSFUL (5m34s) — app-debug.apk produced with libfl_native.so cross-compiled + linked +
stripped for ALL FOUR ABIs (armeabi-v7a, arm64-v8a, x86, x86_64) via CMake+NDK+cargo (Android
rust std targets were added). readelf confirms the NEW JNI symbol
Java_org_florisboard_libnative_FlorisNative_nativeGetTouchOffsets (5a) AND
...nativeRecordTouchHit (6) are exported GLOBAL FUNC in the arm64 .so — this retires the prior
session's flagged catastrophic risk ("wrong JNI symbol = UnsatisfiedLinkError at keyboard load").
:app:testDebugUnitTest 161 tests 0 fail; cargo test --workspace 29 suites green (real_assets_smoke).
The earlier "never compiled here" caveat is fully retired: every change is compiler-, linker-,
test- and (for the arithmetic/lock/oracle pieces) oracle-verified. assembleDebug surfaced nothing
the compile-only check missed. A device/emulator smoke of LIVE keyboard behaviour + clipboard
render remains the ideal last human step (actual on-screen rendering + a live .so load under a real
IME session can only be exercised on a device), but nothing now rests on static review alone.


## Items

| # | Item | Status |
|---|------|--------|
| 1 | Flight-recorder call-site guards + BatteryIndicatorWidget | DONE (iter 1, pushed) |
| 2 | Easter-egg scan → precomputed sets | DONE (iter 2, pushed) |
| 3 | remember dynamicColorScheme ×2 + lazy Snygg uuid | DONE (iter 3, pushed) |
| 4 | NlpManager regex/ZonedDateTime hoists + SentenceEndMatcher tail-scan + pref-write guards | DONE (iter 4, pushed) |
| 5 | Bulk touch offsets + system-dict cache | DONE (build-verify session: 5a+5b shipped & compiled) |
| 6 | recordTouchHit off UI thread / off NLP_ENGINE lock | DONE (build-verify session: lock-split shipped, concurrency-guarded) |
| 7 | Trie max_subtree_frequency pruning | DONE (iter 7, pushed) |
| 8 | Emoji off-main + prewarm; clipboard grid keys + Main collector + off-main bitmap decode | DONE (build-verify session: bitmap decode shipped) |
| 9 | Editor pipeline: BMP fast path, volatile content, cached substrings | DONE (iter 9, pushed) |
| 10 | Updater unmetered check + island throttle; startup ordering | DONE (iter 10, pushed) |

## Environment notes
- Android SDK: FULLY installed (cmdline-tools + platforms;android-36 + build-tools;36.0.0 +
  platform-tools at /opt/android-sdk; NDK 26.3.11579264 auto-fetched by AGP; local.properties
  has sdk.dir=/opt/android-sdk, gitignored).
- Kotlin/Gradle compile verification is NOW POSSIBLE: the network policy was changed to Custom
  with dl.google.com + maven.google.com allowlisted, so AGP 9.0.0 + AndroidX/Compose resolve.
  Maven Central (repo.maven.apache.org) intermittently returns HTTP 429 (Too Many Requests) on
  the shared egress IP; a serialized retry (--max-workers=1 + backoff) converges since Gradle
  caches every successful fetch. First cold build ~converges over a few retries, then warm.
- COMPILE-VERIFIED 2026-09-02 (this session): `./gradlew :app:compileDebugKotlin` BUILD
  SUCCESSFUL with ZERO errors (only benign warnings) — every oracle-verified Kotlin change from
  iters 1-10 compiles. `:app:testDebugUnitTest` = 159 tests, 0 failures, 0 errors.
  `cargo test --workspace --release` = 27 suites green incl. sentinel real_assets_smoke.
  No compile fixes were required, validating the prior oracle-based method.
- Native verify: cargo test --workspace from libnative/ (sentinel real_assets_smoke must stay green).
- Benchmarks: libnative/.../tests/perf_review_bench.rs (-- --ignored --nocapture), utils/perf-proof/*.java.

## Log
- item 8 DONE (2026-09-02, build-verified): clipboard ClipItemView image/video decode moved OFF
  the main thread. Was: BitmapFactory.decodeFile of the FULL-resolution clip synchronously inside
  remember() during composition (per-frame jank + OOM risk on large clips). Now a new
  AsyncClipThumbnail composable measures the cell (onSizeChanged), decodes on Dispatchers.IO via
  produceState with a Loading placeholder (fillMaxWidth + aspectRatio(1f)), and renders the Image
  or the same error text on completion. IMAGE decode uses a bounds pre-pass (inJustDecodeBounds)
  + inSampleSize sized to the cell width (calculateInSampleSize), so a huge clip is never inflated
  at full res yet never blurred below the cell. VIDEO thumbnail logic is byte-identical to the old
  inline path, just off-main; its Videocam badge is passed as the overlay. Guard:
  app/src/test/.../clipboard/ClipInSampleSizeTest.kt (Kotest) — power-of-two, no-blur-below-req,
  maximal-downsample, degenerate-input invariants + an independent brute-force reference across a
  16x12 grid. Verified: :app:compileDebugKotlin + :app:testDebugUnitTest BUILD SUCCESSFUL (161
  tests, 0 fail). Full render fidelity still ideally confirmed on a device, but the decode
  arithmetic is oracle-proven and the async lifecycle compiles. ALL 4 DEFERRED SUB-PARTS DONE.
- item 6 DONE (2026-09-02, build-verified): touch model moved off NLP_ENGINE's RwLock. The
  NlpEngine.touch_model field is now Arc<RwLock<Option<TouchModel>>> (its own lock). New
  record_touch_hit_biometrics(&self) mutates it through a shared engine borrow, so the JNI
  nativeRecordTouchHit takes NLP_ENGINE.read() + touch_model.write() instead of the whole-engine
  write lock — a background key-down no longer stalls in-flight suggest reads on the engine lock.
  The suggest read path snapshots the model once via slip_oracle (a frozen read guard per fuzzy
  pass / per rescoring pass), preserving the old "model frozen for the duration of a suggest"
  consistency; each char-pair comparison is a plain match, not a lock. keys_near kept as a
  per-call convenience for external callers. Lock order is always engine-before-touch_model, so
  no cycle -> no deadlock. Guard: tests/touch_model_lock_split.rs — (a) frozen-model suggest is
  deterministic over 1000 repeats; (b) 4 suggest readers + 2 recorders + 1 layout-uploader
  (engine.write) hammering the JNI deployment shape (Arc<RwLock<NlpEngine>>) all terminate with
  no deadlock/panic and a consistent post-run suggest. Verified: cargo test --workspace --release
  = 29 suites green (real_assets_smoke ok); JNI crate cargo build clean (NlpEngine still
  Send+Sync for the static NLP_ENGINE); :app:compileDebugKotlin BUILD SUCCESSFUL. perf bench
  perf_review_lock_stall updated to the new read+touch-model-lock path.
- item 5b DONE (2026-09-02, build-verified): system user-dictionary shortcut cache.
  DictionaryManager.queryUserDictionary previously did a ContentResolver IPC to the framework
  UserDictionary provider on EVERY keystroke (systemUserDictionaryDao().queryShortcut). Now a
  @Volatile systemShortcutIndex (Map<exact-shortcut, List<UserDictionaryEntry>>) is built once
  from a single queryAll() and served from RAM. querySystemShortcutCached replicates the
  provider selection byte-for-byte: exact case-sensitive shortcut bucket, then locale predicate
  (loc==null when locale null; else loc==null||loc==localeTag||loc==language), preserving
  queryAll's FREQUENCY-DESC order; candidates mapped identically (secondaryText="Snippet"). A
  ContentObserver on UserDictionary.Words.CONTENT_URI (own HandlerThread) re-warms the snapshot
  off-main on any system-dict change; registered in loadUserDictionariesIfNecessary + lazily on
  first query (handles runtime enable), torn down in unloadUserDictionariesIfNecessary. No unit
  oracle (Android ContentProvider/observer lifecycle is not host-testable — the reason it was
  deferred); behaviour-equivalence argued from the SQL selection, all failure paths fall back to
  an empty snapshot (== old failed-IPC result). Verified: :app:compileDebugKotlin +
  :app:testDebugUnitTest BUILD SUCCESSFUL (159 tests, 0 fail). ITEM 5 NOW FULLY DONE.
- item 5a DONE (2026-09-02, build-verified): bulk touch-offset fetch. Added
  HitTester::offsets_for(&[char]) (crake-core) = one (dx,dy) per char, each identical to
  offset_for; new JNI symbol nativeGetTouchOffsets(IntArray)->FloatArray in lib.rs mirrors
  nativeGetTouchOffset's decode (char::from_u32(..).unwrap_or('\0'), full precision, (0,0)
  default) and returns a flat [dx0,dy0,...] of length 2n. FlorisNative.getTouchOffsets wrapper
  (all-zero fallback when unloaded). TextKeyboard.getKeyForPosAdaptive now does ONE crossing per
  tap (IntArray of all key codes) instead of ~30 per-char getTouchOffset calls; indexes offsets
  by key position — behaviour-identical since offset_for is stateless per read. Guard:
  tests/batch_offsets_oracle.rs proves offsets_for == per-char offset_for over learned/unlearned/
  uppercase/non-ASCII/NUL + 2000 random code streams (0 mismatches). Verified: JNI crate
  cargo build OK; cargo test --workspace --release = 28 suites green (real_assets_smoke ok);
  :app:compileDebugKotlin BUILD SUCCESSFUL (build now compiles).
- BUILD-VERIFY session (2026-09-02): Android SDK completed and the FULL Kotlin app compiled for
  the first time. `:app:compileDebugKotlin` BUILD SUCCESSFUL, zero errors — all 11 prior commits'
  Kotlin (KeyboardManager, TextKeyboardLayout, Smartbar, SnyggUi, SnyggPropertySetEditor,
  NlpManager, TextKeyboard, EditorContent, AbstractEditorInstance, EmojiData, Emoji,
  FlorisApplication, ClipboardInputLayout, ClipboardManager, UpdateManager) compile clean.
  `:app:testDebugUnitTest` 159/159 pass. cargo workspace 27 suites green. Now implementing the
  4 deferred sub-parts (5a, 5b, 6, 8) with real build verification. See items 5/6/8 below.
- iter 10 (2026-09-02): item 10 DONE (all 4 sub-parts, no deferral). ALL 10 ITEMS NOW PROCESSED.
  (10a) UpdateManager auto-download now gated on isUnmeteredNetwork(ctx) (ConnectivityManager +
        NET_CAPABILITY_NOT_METERED, defensive runCatching -> false on any error) in addition to the
        autoDownloadOnWifi pref — the pref was named "on Wi-Fi" but nothing checked the connection, so a
        56MB APK could pull over metered data. ACCESS_NETWORK_STATE already in the manifest.
  (10b) Download progress emit throttled from every 1% to >=5% steps (plus a mandatory final 100%).
        Drives _status + a DynamicIslandManager progress update composed inside the Smartbar, so this cut
        ~100 forced island recompositions over the keyboard mid-download to ~20. Oracle:
        utils/perf-proof/IslandThrottleOracle.java, 200,000 random byte streams — new emits are a subset
        of old, >=5 apart, always show 100 exactly once, never more than old. 0 failures.
  (10c) FlorisApplication.init(): block 5 now awaits preferenceStoreLoaded via
        withTimeoutOrNull(3000){ .first{it} } before reading pref-backed state (activeSubtype,
        updateActiveTheme) — stops the race where it preloaded the DEFAULT subtype/theme then redid the
        work; bounded 3s fallback so a datastore stall cannot hang the prewarm. Block 2 cache wipe now
        skips the "loaded" theme-unzip staging dir (listFiles().forEach excluding "loaded") so it no
        longer races block 5 deleting a theme mid-load. The direct-boot (user-locked) wipe at onCreate
        was left as-is: it returns before init() so no fan-out races it.
  Native suite green (27). Kotlin not compiled (proxy blocks AGP); 10b oracle-verified, 10a/10c by static
  review (defensive helper, bounded await, brace-balanced, import still used by the direct-boot path).
- iter 9 (2026-09-02): item 9 DONE (all 3 sub-parts, no deferral).
  (9a) getTextBeforeCursor(n) BMP fast path: if the last min(n,len) chars are all in [U+0020,U+02FF]
       (standalone-grapheme range under any Unicode version; CR/LF and combining/surrogate/ZWJ excluded),
       return tb.takeLast(n) with no ICU BreakIterator pass and no runBlocking; else unchanged ICU
       fallback. Oracle: utils/perf-proof/GetTextBeforeCursorOracle.java diffs it vs java.text
       BreakIterator (authoritative for the simple cases the fast path accepts) over 9,000,057 inputs
       incl. combining/CRLF/surrogate/ZWJ, n in {1,2,3} — 0 mismatches.
  (9b) activeContent getter: expectedContent() was runBlocking { queue.peekNewestOrNull() } (coroutine
       Mutex) on every one of ~98 call sites. Added a @Volatile newest cache in ExpectedContentQueue,
       republished under the lock in the only 3 mutation sites (push/popUntilOrNull/clear); getter now
       does a lock-free volatile read. Removed the now-unused peekNewestOrNull. popUntilOrNull restructured
       to set newest=list.lastOrNull() before returning (same removal semantics as the old early-return).
  (9c) EditorContent's 5 substring getters (textBeforeSelection/selectedText/textAfterSelection/
       composingText/currentWordText) changed from get()= to by lazy(PUBLICATION). EditorContent is an
       immutable data class so each ~256-char substring is computed at most once instead of on every
       access (read 9+ times/keystroke); PUBLICATION mode is lock-free and safe for the idempotent
       cross-thread reads; the cached vals are body properties, not constructor params, so equals/
       hashCode/copy are unchanged.
  Native suite green (27). Kotlin not compiled (proxy blocks AGP); 9a oracle-verified, 9b/9c by static
  review (all queue mutation sites covered; braces balanced; no dangling refs).
- iter 8 (2026-09-02): item 8 PARTIAL. Shipped 5 of 6 sub-parts:
  (8a) EmojiData.get now wraps loadEmojiDataMap in withContext(Dispatchers.IO) — the asset read+parse
       no longer runs on Main (MediaInputLayout's LaunchedEffect); cached, so paid once per path.
  (8b) FlorisApplication prewarms "ime/media/emoji/root.txt" (the panel's path) in addition to the
       locale path (kept — EmojiSuggestionProvider uses it); the cache is path-keyed so root.txt was
       previously a guaranteed miss on first panel open.
  (8c) Emoji.init: value.codePoints().toList() (boxed List<Integer> + boxed .contains per enum entry,
       ~1500 emoji at load) -> value.codePoints().toArray() (int[]) + IntArray.any (primitive compare).
       Same algorithm (first enum entry by ordinal whose id is present, else DEFAULT). Oracle:
       utils/perf-proof/EmojiInitOracle.java, 3,000,013 inputs incl. skin-tone/hair/ZWJ/multi-tone, 0
       mismatches.
  (8d-keys) Clipboard staggered grid: items(items, key = { it.id }) — ClipboardItem.id is the Long PK;
       stops re-composing/re-decoding every visible item when a new clip shifts positions (matches the
       emoji grid which already keys). (8d-collector) Dropped withContext(Dispatchers.Main) in
       ClipboardManager.initializeForContext so the DB-change collector (sort + JNI classify + list
       passes, per copy) runs on ioScope's IO dispatcher, not the UI thread; historyFlow is a StateFlow
       (thread-safe from any thread), updateHistory touches no UI. Removed the now-unused withContext import.
  Native suite green (27). Kotlin not compiled (proxy blocks AGP).
  DEFERRED (recorded): (8d-bitmap) inSampleSize downsample + off-main decode in ClipItemView
  (ClipboardInputLayout.kt ~448-495). Needs a bounds pre-pass, a target size, a produceState(IO) with a
  loading state, and render verification — bitmap sizing/loading UI can only be validated on a device.
  NOTE: 8d-keys already removes the re-decode STORM (the high-frequency jank: every copy re-decoding all
  visible items); the deferred piece addresses the single-item decode size/OOM + moving that one decode
  off-main. Flagged for the device pass.
- iter 7 (2026-09-02): item 7 DONE (fully verified, no deferral). Added TrieNode.max_subtree_freq
  (upper bound on any terminal freq in the subtree), maintained monotonically: insert() bumps it
  during its single descent (freq known up front); boost_or_insert() does a 2nd path-walk with the
  new freq (learning path, off hot read path). collect_top + collect_top_filtered now skip a child
  whose max_subtree_freq < worst top-k freq once top is full (strict `<` preserves the lex tiebreak).
  Guard: tests/trie_pruning_oracle.rs — pruned prefix_search/prefix_search_filtered byte-identical to
  an independent brute-force reference (collect-all, sort freq-desc/lex-asc, truncate) on the shipped
  49,470-word dict (24 prefixes x 7 limits, plus 4 end-letter filters) AND 400 random tries built via
  both insert() and boost_or_insert() with heavy freq ties; plus a max_subtree_freq>=true-subtree-max
  invariant walk. All pass. cargo test --workspace green (27 suites incl. real_assets_smoke).
  PERF (perf_review_bench, shipped dict): prefix_search("s",12) 207->3.3us (~63x); ("s",300)
  412->170us (~2.4x); suggest_with_context("s") 243->21.9us (~11x/keystroke); predict_next_letter_words("s")
  281->8.1us (~35x). Typo suggests (~1ms) unchanged as expected (fuzzy-probe bound, different finding).
- iter 6 (2026-09-02): item 6 PARTIAL. Shipped the part that removes the measured user-visible harm:
  FlorisNative.recordTouchHit is now posted to a file-level single-thread daemon executor
  (touchHitExecutor) instead of running on the UI touch-down handler. That call takes the native
  NLP_ENGINE write lock and blocked the UI thread avg 376us / worst 1.65ms (perf_review_bench
  lock-stall test) behind in-flight suggest/glide reads; nothing on the touch path consumes its result
  (only future taps read the learned bias), so off-thread is behaviour-equivalent. Rust UNCHANGED (native
  record fns still covered by cargo suites; workspace green, 26). Guard: utils/perf-proof/TouchHitDispatchOracle.java
  proves single-thread executor preserves submission order (EMA/Gaussian updates stay in tap order) and
  that capturing args into locals snapshots tap-time values (500k + 100k trials, passed).
  DEFERRED (recorded): moving the touch model off NLP_ENGINE's RwLock. It requires relocating
  NlpEngine.touch_model (written per key-down by record_touch_hit_with_biometrics, READ during suggest by
  keys_near for slip detection) into its own global lock — a nested-lock/deadlock-ordering + suggest-
  consistency change that unit tests cannot prove deadlock-free and that needs the integrated app to
  validate (unavailable here). Note: with recordTouchHit now off the UI thread, the remaining contention is
  background-only (touchhit worker vs suggest workers), OFF the UI critical path — so the deferred split is
  a minor non-user-visible optimization, not the measured harm. Flagged for the device/real-build pass.
- iter 5 (2026-09-02): item 5 PARTIAL. Shipped the fully-safe part: getKeyForPosAdaptive
  (TextKeyboard.kt) now precomputes the top-row membership as an identity HashSet once per tap
  instead of re-scanning arrangement.firstOrNull()?.contains(key) for every key (~260 comparisons/tap
  removed; TextKey has identity equality so HashSet membership == Array.contains, proven by
  utils/perf-proof/TopRowMembershipOracle.java, 5.75M cases, 0 mismatches). Native suite green (26).
  DEFERRED (recorded, not pushed) — both blocked on verification this environment cannot provide:
    (5a) JNI offset batching: the existing getAllTouchOffsets() is NOT behaviour-identical to per-char
         getTouchOffset() (native rounds to 1 decimal `{:.1}` and returns only learned keys, vs
         offset_for full precision + (0,0) default), so batching needs a NEW native symbol
         (nativeGetTouchOffsets). A wrong JNI symbol name = UnsatisfiedLinkError at keyboard load =
         total keyboard failure, undetectable here (no Kotlin compile, no device). Catastrophic-failure
         risk for a modest reward (~30 JNI crossings/tap -> 1); deferred to a real-build/device pass.
    (5b) System user-dictionary ContentObserver cache (DictionaryManager.kt): stateful Android-framework
         lifecycle code (ContentResolver/ContentObserver/Handler) with no machine-verifiable core and
         real crash/leak risk if registration is wrong; cannot compile or run it here. Deferred to a
         device pass. Both are flagged for the eventual assembleDebug/emulator verification alongside
         all the Kotlin changes from iters 1-4.
- iter 4 (2026-09-02): item 4 DONE. NlpManager: hoisted NUM_UNIT_REGEX + SIMPLE_MATH_REGEX and the
  four DateTimeFormatters to file-level vals (were compiled/constructed per keystroke in the suggestion
  pipeline); moved ZonedDateTime.now() out of the unconditional top into only the 4 branches that use it.
  Equality-guarded the two pref writes in autoExpandCollapseSmartbarActions (only set() when the value
  changes — StateFlow dedupes, stored result identical, no per-keystroke datastore-dirty/persist).
  KeyboardManager: replaced SentenceEndMatcher.matches(textBefore) (greedy .* backtracking over 256
  chars) with a hand-rolled endsWithSentenceTerminator tail scan. Oracle: utils/perf-proof/NlpItem4Oracle.java
  — Part A diffs the tail scan vs Java `.*[.?!]\s+$` over 4,000,017 cases incl. newlines/exotic line
  terminators/long whitespace (0 mismatches, incl. the subtle rule that `.` does not cross \n/\r/U+0085/
  U+2028/U+2029 while `\s` does); Part B diffs hoisted-regex vs inline evalSimpleMath over 2,000,000
  exprs (0 mismatches) to prove the pattern strings were copied faithfully. Native suite green (26).
  Kotlin not compiled (proxy blocks AGP). Note: NlpManager base already had a benign +1 brace imbalance
  (brace inside a string/template); my net brace delta is 0.
- iter 3 (2026-09-02): item 3 DONE. Wrapped both dynamicColorScheme calls (SnyggUi.kt) in
  remember(resolvedPrimary, materialYouFlags) — keyed on the RESOLVED primary (systemAccentOrDefault
  reads the live system accent when the accent is Unspecified, so keying on the raw arg would miss a
  system-accent change; MaterialYouFlags is a data class = valid key). Stops two HCT/CAM16 tonal-palette
  solves per recomposition and the whole-tree SnyggTheme.query invalidation on every shift/auto-caps
  transition. Made SnyggSinglePropertySetEditor.uuid `by lazy` — the SecureRandom draw + 36-char format
  no longer runs on the per-query hot path (only the settings theme editor ever reads it). Oracle:
  utils/perf-proof/LazyUuidOracle.java (200k instances + 2000 concurrent trials) confirms lazy uuid is
  per-instance-stable, cross-instance-distinct, concurrency-safe — the contract key(propertySet.uuid)
  needs. Scheme memoization verified statically (M3/materialkolor can't run offline): data-class key,
  resolved-primary keying, no equals/hashCode/serialization dependence on uuid. Native suite green (26).
  Kotlin not compiled (proxy blocks AGP).
- iter 2 (2026-09-02): item 2 DONE. Replaced the 34-group / ~1,750-concat-per-keystroke
  easter-egg trigger scan (TextKeyboardLayout.kt) with two precomputed rule classes
  (EggSuffixRule, EggBoundaryRule) instantiated once at file level; per-keystroke path now
  allocates nothing. Verified two behaviour-preserving collapses: `(comp==k && d==" ")`→`comp in set`
  (every delim list contains " "), and ignoreCase variants are no-ops (tb/comp pre-lowercased, keys
  lowercase). Oracle: utils/perf-proof/EasterEggScanOracle.java — 114,000,000 decisions across all 38
  group matchers, old-expression vs rule-engine decision-identical, PASSED. Perf: EasterEggProof.java
  showed precomputed-set ~109x faster than the interpolation loop (22,745ns→209ns). Native workspace
  suite green (26 suites, real_assets_smoke ok). Kotlin not compiled (proxy blocks AGP). Left the
  separate egg effect at the other LaunchedEffect(activeContent) untouched (out of scope).
- iter 1 (2026-09-02): item 1 DONE. Guards: 3 flight-recorder call sites wrapped in
  `if (prefs.devtools.flightRecorderEnabled.get())` (commitCandidate, backspace, key-down logKeyTap
  — mirrors the existing handleSpace pattern; behavior-identical since logging no-ops when off).
  BatteryIndicatorWidget: bounded 24-char tail match + hoisted BATTERY_KEY_SUFFIXES/SET (no per-keystroke
  full-buffer lowercase); OverchargeBatteryCanvas: 7 Paints + 1 Path hoisted to remember{} and mutated
  in place (was ~2,400 allocs/trigger). Oracle: utils/perf-proof/BatteryMatcherOracle.java — 100,000
  cases, old vs new matcher decision-identical, PASSED. Native workspace suite green (real_assets_smoke ok).
  Kotlin not compiled (proxy blocks AGP — see env notes).
